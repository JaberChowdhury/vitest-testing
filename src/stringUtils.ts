export const makeUpperCase = (txt: string) => txt.toUpperCase();
export const makeLowerCase = (txt: string) => txt.toLocaleLowerCase();
export const makeReverse = (txt: string) => txt.split("").reverse().join("");
